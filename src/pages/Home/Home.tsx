import Parallax from "../../components/parallax/Parallax";

const Home = () => {
  return (
    <section className='section-hero' id='hero'>
      <Parallax />
    </section>
  )
};

export default Home;